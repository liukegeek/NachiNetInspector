# 运行镜像名称（仅 docker-compose.image.yml 使用）
KRL_IMAGE=krlparser:latest

# 对外暴露端口
KRL_PUBLIC_PORT=2026

# 结果保留与上传限制
KRL_TASK_RETENTION=PT12H
KRL_CLEANUP_INTERVAL=PT30M
KRL_MAX_FILE_SIZE=1000MB
KRL_MAX_REQUEST_SIZE=1000MB
KRL_MAX_CONCURRENT_TASKS=1
KRL_MAX_ACTIVE_TASKS=2
